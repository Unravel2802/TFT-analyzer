# Copy to .env and fill in real values. Keys are lowercase because
# pydantic-settings matches field names case-insensitively.
riot_api_key=
database_url=
supabase_url=
supabase_key=
secret_key=
allowed_origins=["http://localhost:5173"]
