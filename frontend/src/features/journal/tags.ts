// Fixed tag vocabulary: aggregation only works if the same mistake always gets
// the same label, so tags are picked from chips, never typed free-form.
export const NOTE_TAGS = [
    'misplayed econ',
    'int',
    'good positioning',
    'clutch',
    'bad rng',
    'tilted',
    'forced comp',
    'greedy roll',
] as const
